/**
 * Card
 */
class Card {
  /**
   * Creates a new Card Object
   * @param {string=} value - optional value
   * @param {string=} suit - optional suit
   */
  constructor(value = values.ace, suit = suits.spades) {
    console.log("Card")
    this.value
    this.suit
  }
}
